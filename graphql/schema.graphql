type movie {
  id: Int!
  title: String!
  rating: Float!
  description_intro: String!
  medium_cover_image: String!
}
type Query {
  movies(limit: Int, rating: Float): [movie]!
  movie(id: Int): movie
}
